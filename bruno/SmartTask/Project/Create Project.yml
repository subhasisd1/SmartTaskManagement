info:
  name: Create Project
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseUrl}}/projects"
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": "Hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
